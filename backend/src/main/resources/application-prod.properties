# Producción: systemd/nginx en host → SERVER_ADDRESS por defecto 127.0.0.1
# Docker: establecer SERVER_ADDRESS=0.0.0.0 para aceptar tráfico del bridge
spring.config.activate.on-profile=prod
server.address=${SERVER_ADDRESS:127.0.0.1}
server.port=${PORT:${SERVER_PORT:8082}}
